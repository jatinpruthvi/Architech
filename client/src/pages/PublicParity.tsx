"use client";

/* India-wide public parity surfaces: editorial authority, consent-aware forms,
   and utility interactions without copied branding, fabricated reviews, or lender claims. */
import Link from "next/link";
import { ArrowUpRight, Check, Calculator, Mail, MapPin, Phone, ShieldCheck, Star } from "lucide-react";
import { useMemo, useState } from "react";
import Reveal from "@/components/architech/Reveal";

function Shell({ kicker, title, children }: { kicker: string; title: React.ReactNode; children: React.ReactNode }) {
  const index = kicker.includes("Money") ? "01" : kicker.includes("Feedback") ? "02" : kicker.includes("Crawlable") ? "03" : kicker.includes("Contact") ? "04" : "05";
  const label = kicker.includes("Money") ? "CALCULATION / EDUCATIONAL" : kicker.includes("Feedback") ? "MODERATION / CONSENT FIRST" : kicker.includes("Crawlable") ? "ROUTE INDEX / REVIEWED" : kicker.includes("Contact") ? "FIELD DESK / PRIVATE BY DEFAULT" : "METHOD / PLACE BEFORE ADDRESS";
  return <div className="bg-paper pt-[78px] text-ink"><section className="border-b border-ink/12 bg-night py-20 text-cream md:py-28"><div className="container"><p className="kicker text-ember">{kicker}</p><h1 className="display mt-6 max-w-4xl text-[clamp(46px,8vw,96px)]">{title}</h1><div className="mt-10 flex items-end gap-4 border-t border-cream/15 pt-4"><span className="index-num text-5xl leading-none text-ember">{index}</span><span className="stamp !text-[10px] text-cream/55">{label}<br />India coverage · city-scoped evidence</span></div></div></section>{children}</div>;
}

const faqs = [
  ["What does Architech verify?", "We keep source, freshness, and RERA context visible where available. Anything that cannot be verified remains clearly marked rather than presented as a fact."],
  ["Can I list a property?", "Yes. Owners, agents, and builders can begin the moderated listing flow. Publication remains subject to source, consent, and review checks."],
  ["Does Architech show reviews?", "Only real, consented, moderated feedback may be displayed. The Phase 1 preview does not seed testimonials or ratings."],
  ["Which cities does Architech cover?", "Architech currently exposes reviewed routes for 12 Indian markets. Each city and locality goes live only when its own source and review context is available."],
];

export function AboutPage() {
  return <Shell kicker="Coverage desk · India" title={<>A property platform built around the <em className="text-ember">place.</em></>}>
    <section className="container grid gap-12 py-20 md:grid-cols-[0.8fr_1.2fr] md:py-28"><Reveal><p className="kicker text-brick">Why Architech</p><h2 className="display mt-5 text-[clamp(34px,5vw,62px)]">Before the listing, there is a <em className="text-brick">neighbourhood.</em></h2></Reveal><Reveal delay={100} className="space-y-6 text-[15px] leading-7 text-ink/70"><p>Architech helps people understand streets, services, and property records across reviewed Indian cities before they decide which address deserves a closer look.</p><p>Our operating rule is simple: show the context, name the source, stamp the freshness, and keep contact private by default. That makes the discovery process slower in the right places and clearer everywhere else.</p><p>We are building for buyers, renters, owners, agents, and builders who prefer a reviewable trail over an inflated count.</p><Link href="/guide/" className="link-rail inline-flex items-center gap-2 stamp !text-[11px] font-semibold text-brick">Read the verification method <ArrowUpRight size={14} /></Link></Reveal></section>
    <section className="border-y border-ink/12 bg-sand/60 py-20 md:py-28"><div className="container grid gap-5 md:grid-cols-3">{[["01","Place","Locality notes, access, schools, and daily movement."],["02","Proof","RERA context, source trails, and review status."],["03","Next step","A masked enquiry or a moderated listing draft."]].map(([n,t,b]) => <Reveal key={n}><article className="border-t-2 border-brick pt-5"><span className="index-num text-4xl text-brick/75">{n}</span><h2 className="mt-8 font-display text-2xl">{t}</h2><p className="mt-3 text-sm leading-6 text-ink/65">{b}</p></article></Reveal>)}</div></section>
    <section className="container py-20 md:py-28"><div className="grid gap-12 md:grid-cols-[0.7fr_1.3fr]"><div><p className="kicker text-brick">Plain answers</p><h2 className="display mt-5 text-[clamp(34px,5vw,60px)]">Questions worth <em className="text-brick">asking.</em></h2></div><div className="border-t border-ink/15">{faqs.map(([q,a]) => <details key={q} className="group border-b border-ink/15 py-5"><summary className="cursor-pointer list-none font-display text-xl group-open:text-brick">{q}</summary><p className="mt-3 max-w-2xl text-sm leading-7 text-ink/65">{a}</p></details>)}</div></div></section>
    <section className="border-t border-ink/12 bg-night py-16 text-cream"><div className="container flex flex-col items-start justify-between gap-6 md:flex-row md:items-center"><div><p className="kicker text-ember">Build with the field office</p><h2 className="display mt-4 max-w-2xl text-4xl">Have a property or a local point of view?</h2></div><Link href="/requirements/" className="clay-fill bg-brick px-5 py-3 stamp !text-[11px] font-semibold text-cream">Share the brief <ArrowUpRight size={14} className="ml-1 inline" /></Link></div></section>
  </Shell>;
}

export function ContactPage() {
  const [sent, setSent] = useState(false);
  return <Shell kicker="Field office · reach the team" title={<>A clear question deserves a <em className="text-ember">clear reply.</em></>}>
    <section className="container grid gap-12 py-20 md:grid-cols-[0.7fr_1.3fr] md:py-28"><Reveal><p className="kicker text-brick">Contact desk</p><h2 className="display mt-5 text-[clamp(34px,5vw,60px)]">Ask about a <em className="text-brick">place.</em></h2><p className="mt-6 max-w-md text-[15px] leading-7 text-ink/65">Use the requirement route for a property brief. For product, partnerships, or editorial questions, leave a message below. Production delivery is kept behind an activation gate until the approved inbox is connected.</p><div className="mt-10 space-y-5 border-t border-ink/15 pt-6 text-sm text-ink/70"><p className="flex gap-3"><MapPin size={17} className="mt-0.5 text-brick" />India coverage desk · digital-first</p><p className="flex gap-3"><Phone size={17} className="mt-0.5 text-brick" />Contact channel activation pending</p><p className="flex gap-3"><Mail size={17} className="mt-0.5 text-brick" />Email channel activation pending</p></div></Reveal><Reveal delay={100}><form onSubmit={(event) => { event.preventDefault(); setSent(true); }} className="relative border-l-4 border-l-brick border-y border-r border-ink/15 bg-sand/45 p-6 md:p-9"><div className="mb-7 flex items-center justify-between border-b border-ink/15 pb-4"><p className="stamp text-brick">Message desk / preview</p><span className="stamp !text-[9px] text-trust">CONSENT GATE · ON</span></div>{sent ? <div className="py-12"><Check className="text-trust" size={24} /><h3 className="display mt-5 text-4xl">Message prepared.</h3><p className="mt-3 max-w-md text-sm leading-7 text-ink/65">The preview captured the interaction without sending personal data. Connect an approved inbox before enabling production delivery.</p><Link href="/requirements/" className="night-fill mt-7 inline-flex bg-night px-5 py-3 stamp !text-[11px] font-semibold text-cream">Use the requirement route</Link></div> : <div className="grid gap-5 md:grid-cols-2"><label className="stamp !text-[10px] font-semibold text-ink/60">First name<input required minLength={2} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Last name<input required minLength={2} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Mobile number<input required inputMode="tel" className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Email<input type="email" className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60 md:col-span-2">Message<textarea required minLength={10} className="mt-2 min-h-36 w-full border border-ink/20 bg-paper p-3 text-sm focus:border-brick focus:outline-none" /></label><p className="text-xs leading-5 text-ink/55 md:col-span-2">No message is transmitted in this Phase 1 preview. Enable an approved contact provider before accepting production submissions.</p><button className="clay-fill w-fit bg-brick px-6 py-3 stamp !text-[11px] font-semibold text-cream md:col-span-2">Prepare message</button></div>}</form></Reveal></section>
  </Shell>;
}

export function FeedbackPage() {
  const [rating, setRating] = useState(0); const [sent, setSent] = useState(false);
  return <Shell kicker="Feedback desk · moderation first" title={<>Tell us what the experience <em className="text-ember">felt like.</em></>}><section className="container grid gap-12 py-20 md:grid-cols-[0.7fr_1.3fr] md:py-28"><Reveal><ShieldCheck className="text-trust" size={28} /><h2 className="display mt-6 text-[clamp(34px,5vw,60px)]">Real voices, <em className="text-brick">when ready.</em></h2><p className="mt-5 max-w-md text-sm leading-7 text-ink/65">Architech never seeds ratings or testimonials. This form is a visual parity surface; moderation storage and consent logging must be activated before submissions are accepted.</p></Reveal><Reveal delay={100}><form onSubmit={(event) => { event.preventDefault(); if (rating) setSent(true); }} className="relative border-l-4 border-l-trust border-y border-r border-ink/15 bg-sand/45 p-6 md:p-9"><div className="mb-7 flex items-center justify-between border-b border-ink/15 pb-4"><p className="stamp text-brick">Feedback ledger / preview</p><span className="stamp !text-[9px] text-trust">MODERATION · LOCKED</span></div>{sent ? <div className="py-12"><Check className="text-trust" size={24} /><h3 className="display mt-5 text-4xl">Thank you for the signal.</h3><p className="mt-3 max-w-md text-sm leading-7 text-ink/65">This preview does not store or publish feedback. Connect moderation storage before enabling submission.</p></div> : <div className="grid gap-5"><fieldset><legend className="stamp !text-[10px] font-semibold text-ink/60">Overall rating</legend><div className="mt-3 flex gap-2" aria-label="Choose a rating">{[1,2,3,4,5].map((value) => <button key={value} type="button" onClick={() => setRating(value)} aria-label={`${value} stars`} className="touch-44 text-2xl text-brick transition-transform hover:scale-110"><Star fill={value <= rating ? "currentColor" : "none"} /></button>)}</div></fieldset><label className="stamp !text-[10px] font-semibold text-ink/60">Your name<input required minLength={2} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Mobile number<input required inputMode="tel" className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Email (optional)<input type="email" className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Your experience (optional)<textarea className="mt-2 min-h-32 w-full border border-ink/20 bg-paper p-3 text-sm focus:border-brick focus:outline-none" /></label><p className="text-xs leading-5 text-ink/55">Preview only. No review is stored, rated, or displayed until moderation and consent are enabled.</p><button disabled={!rating} className="clay-fill btn-solid w-fit bg-brick px-6 py-3 stamp !text-[11px] font-semibold text-cream disabled:cursor-not-allowed">Prepare feedback</button></div>}</form></Reveal></section></Shell>;
}

export function HomeLoanPage() {
  const [amount, setAmount] = useState(2500000); const [years, setYears] = useState(20); const [rate, setRate] = useState(8.75);
  const emi = useMemo(() => { const monthly = rate / 1200; const months = years * 12; return monthly ? amount * monthly * ((1 + monthly) ** months) / (((1 + monthly) ** months) - 1) : amount / months; }, [amount, years, rate]);
  const interest = Math.max(0, emi * years * 12 - amount);
  return <Shell kicker="Money context · not financial advice" title={<>Understand the cost before you choose the <em className="text-ember">home.</em></>}><section className="container grid gap-12 py-20 md:grid-cols-[1fr_0.85fr] md:py-28"><Reveal><Calculator className="text-brick" size={28} /><h2 className="display mt-6 text-[clamp(34px,5vw,60px)]">An educational EMI <em className="text-brick">lens.</em></h2><p className="mt-5 max-w-xl text-[15px] leading-7 text-ink/65">Adjust the principal, tenure, and rate to understand an indicative monthly payment. This is a calculator, not a lender quote, eligibility decision, or recommendation.</p><div className="mt-10 grid gap-6 border-t border-ink/15 pt-7"><label className="stamp !text-[10px] font-semibold text-ink/60">Principal amount<input type="number" min={100000} max={50000000} step={50000} value={amount} onChange={(e) => setAmount(Number(e.target.value))} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Tenure · years<input type="number" min={1} max={30} value={years} onChange={(e) => setYears(Number(e.target.value))} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label><label className="stamp !text-[10px] font-semibold text-ink/60">Interest rate · % p.a.<input type="number" min={0.5} max={20} step={0.05} value={rate} onChange={(e) => setRate(Number(e.target.value))} className="mt-2 h-12 w-full border border-ink/20 bg-paper px-3 text-sm focus:border-brick focus:outline-none" /></label></div></Reveal><Reveal delay={100}><aside className="relative border-l-4 border-l-trust border-y border-r border-ink/15 bg-night p-7 text-cream md:p-9"><div className="flex items-center justify-between border-b border-cream/15 pb-4"><p className="stamp text-ember">Indicative calculation</p><span className="stamp !text-[9px] text-trust">ASSUMPTIONS / VISIBLE</span></div><p className="mt-10 font-display text-5xl">₹{Math.round(emi).toLocaleString("en-IN")}</p><p className="mt-2 text-sm text-cream/60">estimated monthly EMI</p><dl className="mt-10 space-y-4 border-t border-cream/15 pt-5 text-sm"><div className="flex justify-between gap-4"><dt className="text-cream/60">Principal</dt><dd>₹{amount.toLocaleString("en-IN")}</dd></div><div className="flex justify-between gap-4"><dt className="text-cream/60">Indicative interest</dt><dd>₹{Math.round(interest).toLocaleString("en-IN")}</dd></div><div className="flex justify-between gap-4"><dt className="text-cream/60">Total payable</dt><dd>₹{Math.round(amount + interest).toLocaleString("en-IN")}</dd></div></dl><p className="mt-8 text-xs leading-5 text-cream/50">Rates and outputs are illustrative. Confirm terms directly with an approved lender.</p></aside></Reveal></section><section className="border-y border-ink/12 bg-sand/60 py-20"><div className="container grid gap-5 md:grid-cols-4">{[["01","Submit a brief"],["02","Review eligibility"],["03","Compare approved terms"],["04","Decide with documents"]].map(([n,t]) => <article key={n} className="border-t-2 border-brick pt-4"><p className="index-num text-3xl text-brick/75">{n}</p><h3 className="mt-6 font-display text-xl">{t}</h3><p className="mt-2 text-sm leading-6 text-ink/60">Provider and legal activation required.</p></article>)}</div></section><section className="container py-16"><div className="border border-ink/15 bg-paper p-7 md:flex md:items-center md:justify-between"><div><p className="kicker text-brick">Eligibility enquiry</p><h2 className="display mt-3 text-3xl">Need a human explanation?</h2><p className="mt-2 text-sm text-ink/60">Use the requirement route until an approved finance partner is connected.</p></div><Link href="/requirements/" className="clay-fill mt-5 inline-flex bg-brick px-5 py-3 stamp !text-[11px] font-semibold text-cream md:mt-0">Share a brief <ArrowUpRight size={14} className="ml-1 inline" /></Link></div></section></Shell>;
}

const sitemapGroups = [
  ["Discover", [["Buy homes across India", "/buy/"], ["Search all homes", "/search/"], ["Developers", "/developers/"], ["Investment context", "/investment/"]]],
  ["Trust & context", [["Field notes", "/guide/"], ["Requirements", "/requirements/"], ["Saved homes", "/saved/"], ["Saved searches", "/saved-searches/"]]],
  ["Property paths", [["List a property", "/list-property/"], ["City & locality hubs", "/buy/"], ["Home loan calculator", "/home-loan/"], ["Blogs / field notes", "/blogs/"]]],
  ["Company", [["About Architech", "/about-us/"], ["Contact desk", "/contact-us/"], ["Feedback", "/review/"], ["Privacy", "/privacy/"]]],
] as const;

export function HtmlSitemapPage() {
  return <Shell kicker="Crawlable index · reviewed India routes" title={<>Every useful path should be <em className="text-ember">findable.</em></>}><section className="container py-20 md:py-28"><div className="mb-12 grid gap-6 border-l-2 border-brick pl-6 md:grid-cols-[1fr_auto] md:items-end"><div><p className="kicker text-brick">Route register</p><h2 className="display mt-3 text-4xl">A small map, kept <em className="text-brick">legible.</em></h2></div><p className="stamp max-w-xs !text-[10px] leading-5 text-ink/55">Reviewed public paths only. Facets and private workspaces stay out of the index until their source and access rules are ready.</p></div><div className="grid gap-10 md:grid-cols-2">{sitemapGroups.map(([title, links]) => <Reveal key={title}><section className="border-t-2 border-brick pt-5"><p className="stamp text-brick">{title}</p><ul role="list" className="mt-6 space-y-4">{links.map(([label, href]) => <li key={href + label}><Link href={href} className="link-rail flex items-center justify-between gap-4 font-display text-xl text-ink hover:text-brick"><span>{label}</span><ArrowUpRight size={16} /></Link></li>)}</ul></section></Reveal>)}</div><div className="mt-16 border border-ink/15 bg-sand/50 p-6 text-sm leading-7 text-ink/65"><p className="flex gap-3"><ShieldCheck size={18} className="mt-1 shrink-0 text-trust" />This HTML sitemap exposes reviewed Architech-owned routes. Faceted search URLs remain noindex until verified inventory and canonical rules are available.</p></div></section></Shell>;
}
