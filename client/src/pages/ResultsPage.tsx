/* Architech Editorial Terracotta: discovery surface with URL-ready filters, scan-friendly cards, and map as enhancement rather than dependency. */
import { useState } from "react";
import { ArrowUpRight, ChevronDown, Filter, Map, Search, SlidersHorizontal } from "lucide-react";
import { Link } from "wouter";
import PropertyCard, { properties } from "../components/architech/PropertyCard";

export default function ResultsPage() {
  const [mapMode, setMapMode] = useState(false);
  return <div className="min-h-screen bg-paper pt-[76px] text-ink"><section className="border-b border-ink/10 bg-limestone py-12"><div className="container"><div className="flex flex-wrap items-center gap-2 text-xs text-ink/55"><Link href="/" className="hover:text-clay">Home</Link><span>/</span><span>Search homes</span></div><div className="mt-9 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="arch-line text-[11px] font-semibold uppercase tracking-[.2em] text-clay">A considered shortlist</p><h1 className="mt-5 font-display text-5xl font-medium leading-[.92] tracking-[-.06em] md:text-6xl">Homes in Ahmedabad</h1><p className="mt-4 text-sm text-ink/60">128 homes with clear facts, locality context, and verified source notes.</p></div><button className="inline-flex items-center gap-2 border border-ink/20 bg-paper px-4 py-3 text-sm font-semibold hover:border-clay"><Search size={16} /> Paldi <ChevronDown size={15} /></button></div></div></section>
    <section className="container py-8"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-ink/10 pb-5"><div className="flex flex-wrap gap-2"><button className="inline-flex items-center gap-2 bg-ink px-4 py-2.5 text-xs font-semibold text-paper"><Filter size={14} /> Buy</button><button className="inline-flex items-center gap-2 border border-ink/15 px-4 py-2.5 text-xs font-semibold hover:border-clay">₹1 Cr – ₹4 Cr <ChevronDown size={14} /></button><button className="inline-flex items-center gap-2 border border-ink/15 px-4 py-2.5 text-xs font-semibold hover:border-clay">2+ BHK <ChevronDown size={14} /></button><button className="inline-flex items-center gap-2 border border-ink/15 px-4 py-2.5 text-xs font-semibold hover:border-clay">More filters <SlidersHorizontal size={14} /></button></div><div className="flex items-center gap-3"><span className="hidden text-xs text-ink/45 md:inline">Sort: Recommended</span><button onClick={() => setMapMode(!mapMode)} className="inline-flex items-center gap-2 border border-ink/15 px-4 py-2.5 text-xs font-semibold hover:border-clay"><Map size={14} /> {mapMode ? "List view" : "Map view"}</button></div></div><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_330px]"><div><div className="mb-6 flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[.15em] text-ink/45">Showing 1–3 of 128 homes</p><span className="text-xs text-ink/45">Updated throughout the day</span></div><div className="grid gap-5 sm:grid-cols-2">{properties.map((property) => <PropertyCard key={property.id} property={property} />)}</div><div className="mt-10 flex items-center justify-between border-t border-ink/10 pt-6"><span className="text-sm text-ink/55">Page 1 of 43</span><button className="inline-flex items-center gap-2 text-sm font-semibold text-clay">Next page <ArrowUpRight size={15} /></button></div></div><aside className={`relative min-h-[460px] overflow-hidden bg-[#d0c7b7] ${mapMode ? "block" : "hidden lg:block"}`} aria-label="Map preview"><div className="absolute inset-0 opacity-60" style={{backgroundImage: "linear-gradient(32deg, transparent 47%, rgba(39,34,28,.17) 48%, transparent 49%), linear-gradient(120deg, transparent 47%, rgba(39,34,28,.12) 48%, transparent 49%), linear-gradient(rgba(247,241,231,.6) 1px, transparent 1px), linear-gradient(90deg, rgba(247,241,231,.6) 1px, transparent 1px)", backgroundSize: "auto, auto, 36px 36px, 36px 36px"}} /><div className="absolute left-[28%] top-[32%] grid h-10 w-10 place-items-center rounded-full bg-clay text-paper shadow-lg"><MapPinIcon /></div><div className="absolute right-[25%] top-[52%] grid h-10 w-10 place-items-center rounded-full bg-ink text-paper shadow-lg"><MapPinIcon /></div><div className="absolute inset-x-5 bottom-5 border border-ink/10 bg-paper/95 p-5 backdrop-blur"><p className="text-[10px] font-semibold uppercase tracking-[.18em] text-clay">Map as context</p><p className="mt-2 text-sm leading-6 text-ink/65">Explore the shape of the neighbourhood without losing the list.</p></div></aside></div></section>
  </div>;
}
function MapPinIcon() { return <span className="block h-3 w-3 rounded-full border-2 border-paper" />; }
